// Escreva uma função que recebe 2 numeros e retorne o maior deles.

function max(x, y) {
  return x > y ? x : y; // se X for maior que Y retorna X se não Y
 }
 
// const max2 = (x, y)  => x > y ? x : y;

console.log(max(10,9 ));





