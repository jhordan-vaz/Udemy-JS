function createPerson(nome, sobrenome, idade, cpf, telefone, endereco) {
  
}


function Calculadora(operador, a, b) {
  if(operador === "+") return a + b;
  if(operador === "-") return a - b;
  if(operador === "*") return a * b;
  if(operador === "/") return a / b;
}

console.log(Calculadora("-", 50, 30));